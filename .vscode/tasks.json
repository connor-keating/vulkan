{
    "version": "2.0.0",
    "tasks": [
        {
            // This task only works if you have opened VSCode using shell.bat
            // I could configure it to run that first, but I don't want to start/restart the shell everytime
            "type": "cppbuild",
            "label": "Build game",
            "command": "build.bat",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$msCompile"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ]
}